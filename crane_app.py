import streamlit as st import datetime from fpdf import FPDF # Set web page title and icon optimized for mobile views st.set_page_config(page_title="Crane Inspection", page_icon="🏗️", layout="centered") st.title("🏗️ Overhead Crane Condition Report") st.write("Complete the form on your mobile device. Click the button at the bottom to download a professional PDF report.") # --------------------------------------------------------- # SECTION 1: ASSET & MANUFACTURER INFORMATION # --------------------------------------------------------- st.header("1. Asset & Manufacturing Information") manufacturer = st.text_input("Manufacturer", placeholder="e.g., Demag, Konecranes") make_model = st.text_input("Make / Model") serial_no = st.text_input("Serial Number") crane_id = st.text_input("Crane ID / Tag No.") capacity = st.text_input("Capacity (Tons / SWL)") location = st.text_input("Facility Location / Bay") inspector = st.text_input("Inspector Name") inspection_date = st.date_input("Inspection Date", datetime.date.today()) status_options = ["Safe to Operate", "Conditional (Monitor)", "Out of Service (Fail)"] overall_status = st.selectbox("Overall Operational Status", status_options) st.markdown("---") # Helper function optimized for single-column mobile view layouts def mobile_inspection_row(component_name): st.subheader(component_name) result = st.radio( f"Status for {component_name}", ["Pass", "Fail", "N/A"], horizontal=True, key=f"status_{component_name}" ) notes = st.text_input( "Notes / Deficiencies", placeholder="Enter deficiencies or notes if any...", key=f"notes_{component_name}" ) st.markdown("---") return {"Status": result, "Notes": notes} # --------------------------------------------------------- # INSPECTION CATEGORIES # --------------------------------------------------------- report_data = {} st.header("2. General Crane Structure & Components") st.caption("Main bridge girders, runway beams, end trucks, tracks, bumpers, and welds.") report_data["General Structure"] = mobile_inspection_row("General Structure") st.header("3. Electric Hoist & Components") st.caption("Hoist drum, load chains/ropes, hooks, safety latches, trolley, and limit switches.") report_data["Electric Hoist"] = mobile_inspection_row("Electric Hoist") st.header("4. Mechanical Components") st.caption("Holding brakes, drive/idler wheels, bearings, couplings, sheaves, and open gears.") report_data["Mechanical Components"] = mobile_inspection_row("Mechanical Components") st.header("5. Electrical Components") st.caption("Motors, control panels, switchgear, contactors, pendants/remotes, and emergency stops.") report_data["Electrical Components"] = mobile_inspection_row("Electrical Components") st.header("6. Crane Supply System") st.caption("Main disconnects, conductor rails, festoon systems, energy chains, and grounding.") report_data["Crane Supply System"] = mobile_inspection_row("Crane Supply System") # --------------------------------------------------------- # FINAL RECOMMENDATIONS & ACTION ITEMS # --------------------------------------------------------- st.header("7. Recommendations & Action Items") recommendations = st.text_area( "Enter corrective actions, parts required, or follow-up inspection timelines:", placeholder="e.g., Schedule immediate replacement for the hoist limit switch." ) st.markdown("---") # --------------------------------------------------------- # PDF GENERATION ENGINE # --------------------------------------------------------- def generate_pdf(): pdf = FPDF() pdf.add_page() # Document Header pdf.set_font("Helvetica", "B", 16) pdf.cell(0, 10, "OVERHEAD CRANE CONDITION REPORT", ln=True, align="C") pdf.ln(5) # Metadata Box pdf.set_font("Helvetica", "B", 11) pdf.cell(0, 7, "1. ASSET & INSPECTION INFORMATION", ln=True) pdf.set_font("Helvetica", "", 10) # Information fields info_fields = [ ("Date of Inspection:", str(inspection_date)), ("Inspector Name:", inspector), ("Overall Status:", overall_status.upper()), ("Manufacturer:", manufacturer), ("Make / Model:", make_model), ("Serial Number:", serial_no), ("Crane ID / Tag No.:", crane_id), ("Capacity (SWL):", capacity), ("Facility Location:", location) ] for label, val in info_fields: pdf.set_font("Helvetica", "B", 10) pdf.cell(45, 6, label, border=0) pdf.set_font("Helvetica", "", 10) pdf.cell(0, 6, val if val else "N/A", ln=True, border=0) pdf.ln(10) # Component Table Header pdf.set_font("Helvetica", "B", 11) pdf.cell(0, 7, "2. COMPONENT STATUS BREAKDOWN", ln=True) pdf.set_font("Helvetica", "B", 10) pdf.cell(60, 8, "Inspection Category", border=1, align="L") pdf.cell(25, 8, "Status", border=1, align="C") pdf.cell(105, 8, "Notes / Deficiencies", border=1, ln=True, align="L") # Component Rows pdf.set_font("Helvetica", "", 10) for category, content in report_data.items(): pdf.cell(60, 8, category, border=1) pdf.cell(25, 8, content["Status"], border=1, align="C") note_text = content["Notes"] if content["Notes"] else "No defects noted." pdf.cell(105, 8, note_text, border=1, ln=True) pdf.ln(10) # Recommendations Section pdf.set_font("Helvetica", "B", 11) pdf.cell(0, 7, "3. ACTION ITEMS & RECOMMENDATIONS", ln=True) pdf.set_font("Helvetica", "", 10) rec_text = recommendations if recommendations.strip() else "No corrective actions listed." pdf.multi_cell(0, 6, rec_text, border=1) # Footer Note pdf.ln(15) pdf.set_font("Helvetica", "I", 8) pdf.cell(0, 5, "This document serves as an official equipment health log record.", align="C") return pdf.output() # --------------------------------------------------------- # MOBILE EXPORT BUTTON (WITH SERIAL NUMBER FILE NAMING) # --------------------------------------------------------- st.header("📋 Export Completed PDF") # Clean up input text so they don't break phone file systems (removes spaces/special characters) clean_id = "".join(x for x in crane_id if x.isalnum() or x in ('-', '_')).strip() clean_serial = "".join(x for x in serial_no if x.isalnum() or x in ('-', '_')).strip() # Create dynamic name: e.g., "Inspection_CRANE-A_SN-12345_2026-09-13.pdf" name_parts = ["Inspection"] if clean_id: name_parts.append(clean_id) if clean_serial: name_parts.append(clean_serial) name_parts.append(str(inspection_date)) mobile_filename = f"{'_'.join(name_parts)}.pdf" try: pdf_bytes = generate_pdf() st.download_button( label="📄 Download Official PDF Report", data=pdf_bytes, file_name=mobile_filename, mime="application/pdf", use_container_width=True, type="primary" ) except Exception as e: st.error("Generating form options. Make sure to input values above.")
